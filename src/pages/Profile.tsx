import Header from '../Header';

function Profile() {
  return (
    <div>
      <h1>
        <Header pageTitle="Profile" showSearchIcon={ false } />
      </h1>
    </div>
  );
}

export default Profile;
